/*window.onload = function () {
    var user_str = document.getElementById("userd");
    var pwd_str = document.getElementById("pwdd");
    user_str.onblur = function () {
        var user_Value = user_str.value;
        var pwd_Value = pwd_str.value;
        alert(user_Value + pwd_Value);
        var uPattern = /^[a-zA-Z0-9]{5,11}$/;
        if (uPattern.test(user_Value)) {
            alert("格式正确!");
        }else {
            alert("账号必须是由字母数字组成，且长度必须大于5小于11！");
        }
        if (uPattern.test(pwd_Value)) {
            alert("格式正确!");
        }else {
            alert("密码必须是由字母数字组成，且长度必须大于5小于11！");
        }
    }
}*/


